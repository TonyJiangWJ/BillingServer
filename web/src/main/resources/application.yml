# Server settings
server:
    port: 1310
    address: localhost
    tomcat:
        uri-encoding: utf-8
# SPRING PROFILES
spring:
#    spring.jackson.default-property-inclusion=NON_NULL
    # HTTP ENCODING
    http:
        encoding.charset: UTF-8
        encoding.force: true
        encoding.enabled: true
    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/my_2017_cost?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false
      username: root
      password: fixture

# MyBatis
mybatis:
    typeAliasesPackage: com.tony.entity
    mapperLocations: classpath:/sqlMapper/*SqlMap.xml
    configLocation: classpath:/mybatis-config.xml

# LOGGING
